<%= form_for(@locale) do |f| %>
  <% if @locale.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@locale.errors.count, "error") %> prohibited this locale from being saved:</h2>

      <ul>
      <% @locale.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :primary_locale %><br />
    <%= f.check_box :primary_locale %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
